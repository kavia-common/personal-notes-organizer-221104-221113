# PUBLIC INTERFACE for environment variables consumed by the React app
# Set your backend base URL (without trailing slash). Defaults to http://localhost:3001
REACT_APP_BACKEND_URL=http://localhost:3001
# Alternative variable name; if both are missing, default is used.
REACT_APP_API_BASE=
